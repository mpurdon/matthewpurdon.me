{
  "name": "matthewpurdon-me",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-og.mjs && astro build && node scripts/generate-feed.mjs && node scripts/generate-sitemap.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name matthewpurdon-me"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "satori": "^0.26.0"
  },
  "devDependencies": {
    "@astrojs/react": "^5.0.7",
    "@vitejs/plugin-react": "^4.3.4",
    "astro": "^6.4.6",
    "vite": "^6.0.0",
    "wrangler": "^4.99.0"
  }
}
